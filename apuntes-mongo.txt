db => mostrara en que base de datos estamos trabajando
use NOMBRE_DB => nos moveremos a esa base de datos y si no existe la creará.
show dbs => mostrará todas las bases de datos que tenemos.
db.dropDatabase() => borrará la base de datos que este ubicado
db.TABLA.insertOne({...}) => insertara un nuevo registro en esa coleccion (TABLA)en la bd y si no existe, la creara
db.TABLA.find() => listará los registros de la tabla.
//LIKE
db.TABLA.find({columna: /x/}) => en cualquier parte de la columna estara la letra x

docker build . --tag express-con-mongo > construye la imagen de tu proyecto, el . indica todo el proyecto, --tag es para darle un nombre
docker run -it -p 5000:3000 express-con-mongo > manda a correr el docker, -it observa el docker, -p 5000:3000 indica el puerto entrante y saliente
docker rmi express-con-mongo --force > eliminamos la imagen
docker-compose up > levanta el docker pero con contenedores?
docker exec -it 73b bash > ejecuta el contenedor de mongo en primer plano(-it) con los 3 primeros digitos del id(73b) bash